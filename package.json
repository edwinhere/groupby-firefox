{
  "name": "groupby",
  "version": "0.1.0",
  "description": "Firefox extension that groups tabs by domain with a pluggable grouping strategy interface.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.23.1",
    "typescript": "5.6.3",
    "vitest": "2.1.4"
  }
}
